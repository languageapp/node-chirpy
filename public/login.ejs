<!DOCTYPE html>
<html ng-app="chirpy">
  <head>
    <title>Chirpy</title>
    <link rel="shortcut icon" href="http://cdn.animals-pics.com/pictures/www.bluebirdfinance.com/wp-content/uploads/2011/10/Original-Blue-Bird-Logo1.jpg"/>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.96.1/css/materialize.min.css">
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js"></script>
    <script type="text/javascript" src="/components/angular/angular.min.js"></script>
    <script type="text/javascript" src="/js/app.js"></script>
  </head>

  <body>
    <header class="col s12 z-depth-2">
      <% if (user) { %>
        Log Out
      <% } else  {%>
        Sign Up
        Log In
      <% } %>
    </header>
    <main>


      <div class="container">
        <div class="row">

          <section ng-controller="LogInController as loginCtrl">
            <form name="signUpForm" class="chirpy-form" ng-submit="signUpForm.$valid && loginCtrl.signUp()" novalidate>

              <p>{{loginCtrl.serverMessage}}</p>

              <fieldset>
              <div class="field input-field">
                Email <br />
                <input type="email" ng-model="loginCtrl.email" name="email" required />
              </div>

              <div class="field input-field">
                Password <br />
                <input type="password" ng-model="loginCtrl.password" name="password" required />
              </div>

              <div class="field input-field">
                Confirm Password <br />
                <input type="password" ng-model="loginCtrl.confirmation" name="confirmation" required />
              </div>

              !! -- Form valid? {{signUpForm.$valid}} -- !!

              <div class="actions">
                <input type="submit" value="Sign Up" />
              </div>
              </fieldset>
            </form>
          </section>



        </div>

      </div>


    </main>
  </body>



</html>
